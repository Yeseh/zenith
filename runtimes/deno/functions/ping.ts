// This serves as a placeholder function for user-provided functions
// Will be deleted during image building
export default (_: Request): Response => {
    return new Response(`Zenith is running!`, {status: 200})
}